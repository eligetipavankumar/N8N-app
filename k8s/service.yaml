apiVersion: v1
kind: Service
metadata:
  name: n8n-service
spec:
  type: NodePort   # or LoadBalancer
  selector:
    app: n8n
  ports:
    - protocol: TCP
      port: 5678        # Service port
      targetPort: 5678  # Container port
      nodePort: 30001   # Optional, for NodePort access
